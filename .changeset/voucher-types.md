---
"@tuler/node-libcmt": patch
---

Export named `Voucher` and `DelegateCallVoucher` types for the `emitVoucher` and `emitDelegateCallVoucher` arguments, so consumers can import and reference them directly instead of relying on inline anonymous object types.
